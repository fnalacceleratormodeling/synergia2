This example creates and populates a bunch and uses
routines to force a Diagnostics_particle output of the
bunch.

It will use openPMD output if that is enabled in the build.
otherwise it will use legacy HDF5.